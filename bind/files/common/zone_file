$TTL 1d
$ORIGIN {{ origin }}.
@		IN	SOA	{{ salt.grains.item('fqdn')['fqdn'] }}.	{{ email }}.	(
			{{ salt.status.time('%s') }}
			1200
			600
			120
			600 )

		IN	NS	{{ salt.grains.item('fqdn')['fqdn'][0] }}.
dns		IN	A	{{ salt.grains.item('fqdn_ip4')['fqdn_ip4'][0] }}
