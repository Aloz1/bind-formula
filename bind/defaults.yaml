bind:
  pkgs:
    - bind

  service_name: named

  key_extension: named.key
  zone_db_extension: zone.db

    # Default users
  user: named
  group: named

  # Default Modes
  modes:
    dir: 750
    file: 640

  template_dir: 'salt://bind/files/default'
